export type themeStructure = {
  background: {
    primary: string;
    secondary: string;
    tertiary: string;
  };
  font: {
    primary: string;
    secondary: string;
    tertiary: string;
  };
  error: {
    primary: string;
  };
};
