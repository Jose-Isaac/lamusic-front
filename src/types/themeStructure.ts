export type themeStructure = {
  background: {
    primary: string;
    secondary: string;
    tertiary: string;
  };
  font: {
    primary: string;
    secondary: string;
  };
  error: {
    primary: string;
  };
};
